// Minimal next-intl config required by getMessages()/next-intl
// Use an ESM default export so next-intl can load it in the app router runtime.

export default {
  locales: ['es', 'en'],
  defaultLocale: 'es',
}
